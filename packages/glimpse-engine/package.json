{
  "name": "@glimpse/engine",
  "version": "0.1.0",
  "description": "High-performance AI video interaction engine.",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "tsup src/index.ts --format esm,cjs --watch --dts",
    "build": "tsup src/index.ts --format esm,cjs --clean --dts",
    "lint": "eslint src"
  },
  "devDependencies": {
    "tsup": "^7.2.0",
    "typescript": "^5.2.2"
  }
}
